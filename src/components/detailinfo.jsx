import React from 'react';
import _ from 'lodash';

const DetailInfo = (props) => {
    const { countrydata, name:countryname} = props
    return(
        <div>
        </div>
    );
}

export default DetailInfo;